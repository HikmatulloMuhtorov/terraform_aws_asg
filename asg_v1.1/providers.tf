provider "aws" {
  region = "us-east-1"
}

# Reading what is alias in providers?
# how to create same resources across multiple regions by one terraform apply.
# can I manage resources across aws and Digital Ocean at the same time